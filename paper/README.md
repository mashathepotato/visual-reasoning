# Paper source status

The TeX files in this directory are legacy working drafts and are not currently
submission-ready. Unsupported comparative results were removed from
`neurips.tex` on 2026-08-02 pending matched multi-seed reruns.

A clean build is not yet possible because the repository does not contain the
original bibliography, NeurIPS style file, checklist, or referenced figure tree.
These inputs must be recovered and reviewed before generating a new PDF. The
submitted NeurIPS PDF is also absent, so prompt-injection removal has only been
verified for searchable repository sources and the standalone diagram PDFs.

Do not restore legacy table values unless each cell is backed by a committed
configuration, per-seed result, checkpoint-selection rule, and aggregation output.
